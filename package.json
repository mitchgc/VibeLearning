{
  "name": "vibelearning",
  "version": "1.0.0",
  "description": "Universal Software Training Assistant - Real-Time Learning Layer for SaaS Applications",
  "private": true,
  "workspaces": [
    "extension",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev:extension & npm run dev:backend",
    "dev:extension": "cd extension && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:extension && npm run build:backend",
    "build:extension": "cd extension && npm run build",
    "build:backend": "cd backend && npm run build",
    "test": "npm run test:extension && npm run test:backend",
    "test:extension": "cd extension && npm test",
    "test:backend": "cd backend && npm test"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}